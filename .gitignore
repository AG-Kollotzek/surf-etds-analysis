# macOS / editor / build noise
.DS_Store
__pycache__/
*.pyc
.venv/
.pytest_cache/
.idea/

# Generated by the pipeline - rebuild with `python paper_pipeline.py` (~1 min).
# Exact directory rules on purpose: a pattern like *.pdf or *.csv would swallow the
# measurement protocol, the raw ETD logs, the zoom configs and the retired tables.
paper_data/process_v2/
paper_results/
.export_staging/

# Handover bundle - rebuild with `python export_bundle.py`
SURF_ETD_Evaluation_*.zip
SURF_ETD_Evaluation_*/
